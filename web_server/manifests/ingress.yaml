apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kube-web-server-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: kube-web-server-svc
            port:
              number: 1234