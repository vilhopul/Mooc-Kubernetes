apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: project-claim
  namespace: project
spec:
  storageClassName: project-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi