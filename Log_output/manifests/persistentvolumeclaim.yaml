apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: log-output-claim
  namespace: exercises
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi